<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SumTheNumbersGameXamarin.Views.SettingsPageView">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Label Text="Count of numbers: " />
            <Label BindingContext="{x:Reference countSlider}"
                   Text="{Binding Value, StringFormat='The angle is {0:F0} degrees'}" Grid.Column="1"/>
            <Slider x:Name="countSlider" Grid.Row="1"
                    Maximum="10"
                    Minimum="4"
                    Value="{Binding Count}"
                    />
        </Grid>
    </ContentPage.Content>
</ContentPage>